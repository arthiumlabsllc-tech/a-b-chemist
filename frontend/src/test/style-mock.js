// Styles are compiled by Tailwind at build time; a unit test only needs the
// import not to throw. Mapping CSS to this object is what lets a test import a
// module that transitively imports `globals.css`.
module.exports = {};
